packages: grisette.cabal
allow-newer: base

-- Same native fork as the owning p4check2 project: exact 64af8a97 base plus
-- direct registered-free-symbol model projection. Never use the browser fork
-- or a cached source tree as the native development authority.
packages: ../sbv-native/sbv.cabal

package grisette
  tests: True

constraints: sbv == 13.5
